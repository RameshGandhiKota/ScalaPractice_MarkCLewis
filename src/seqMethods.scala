
object seqMethods extends App{
  
  val nums = List(1,5,7,5,4,3,6,8,5,4,3,6,7,8)
  val str = "This is a test"
  
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  println("Length of string and Numbers: " + str.length + "" + nums.length)
  
}